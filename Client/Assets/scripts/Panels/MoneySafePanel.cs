using UnityEngine;
using UnityEngine.UI;

public class MoneySafePanel : MonoBehaviour
{
    [SerializeField] private Text totalMoney;
    [SerializeField] private Text userInputMoney;
    [SerializeField] private Text safeMoney;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
